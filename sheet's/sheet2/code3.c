// write a c program to check weather a no. is even or odd using conditional operator.
# include <stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if (a%2==0)
    {
        printf("the no. is even");
    }
    else
    {
        printf("the no. is odd");
    }

        return 0;
}