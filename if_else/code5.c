// to check no is even or odd
#include<stdio.h>

int main()
{
    //by if else
    
    int a;
    scanf("%d",&a);
    if (a%2==0)
    {
        printf("the no. is even");
    }
    else
    {
        printf("the no. is odd");
    }

        return 0;
}
